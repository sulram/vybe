# hello.vy · the smallest patch: one node, no scenes.
# With no scene declared, the last node that draws is what shows.
#
#   vybe run examples/patches/hello/hello.vy

dot = circle .18 soft .6  hue 200 drift 30  wave .2 .16hz .21hz
